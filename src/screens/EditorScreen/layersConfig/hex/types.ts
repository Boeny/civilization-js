export enum HEX_TYPE {
    ocean,
    sea,
    desert,
    dirt,
    meadow,
    tundra,
    snow,
    ice,
    mountain,
    hill,
}

export enum CREATE_MODE {
    fitImage,
    fitScreen,
    free,
}
