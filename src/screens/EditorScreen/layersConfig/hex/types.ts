export enum HEX_TYPE {
    water,
    desert,
    dirt,
    meadow,
    tundra,
    snow,
    ice,
    mountain,
    hill,
}

export enum CREATE_MODE {
    fitImage,
    fitScreen,
    center,
    free,
}
