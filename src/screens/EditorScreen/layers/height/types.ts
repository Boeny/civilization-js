export enum HEX_TYPE {
    empty,
    desert,
    dirt,
    meadow,
    tundra,
    snow,
    ice,
    mountain,
    hill,
}

export enum CREATE_MODE {
    fitImage,
    fitScreen,
    center,
    free,
}
